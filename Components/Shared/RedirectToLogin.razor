@inject NavigationManager Navigation
@code {
    protected override void OnInitialized()
    {
        // 確保導向 /login，且使用 replace 以保持網址乾淨
        Navigation.NavigateTo("/login", replace: true);
    }
}
